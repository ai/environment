#!/bin/sh
# Работа с личной папкой.

USER=ai
DIR=/home/$USER
STORE=$DIR/.Личное
LINK=$DIR/Рабочий\ стол/Личное

if [ -e "$LINK" ]; then
    # Прячем
    chown -R root:root "$STORE"
    rm "$LINK"
else
    # Показываем
    echo $USER
    chown -R $USER:$USER "$STORE"
    ln -s "$STORE" "$LINK"
fi
