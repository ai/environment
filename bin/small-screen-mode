#!/bin/sh
# Change settings when I move to 13" laptop screen

if [[ "$1" == "enable" ]]; then
  # Reduce font size in GNOME

  # Enable Hide Titlebar extension and use native title bar in VS Code
else

fi
